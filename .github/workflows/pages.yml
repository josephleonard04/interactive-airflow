# Publish the app to GitHub Pages on every push to main.
#
# The point is that a participant needs a URL, not a checkout. They open the
# link on their own computer, whenever they like; nothing is installed, no
# server of ours has to be awake, and the page cannot go stale because this
# rebuilds it from main every time.
#
# ONE-TIME SETUP (has to be done in the browser, once):
#   Settings -> Pages -> Build and deployment -> Source: "GitHub Actions"
# After that the URL is https://<user>.github.io/<repo>/ and this keeps it fresh.
#
# Optional: set a LOG_ENDPOINT repository secret and finished sessions are POSTed
# there as well as downloaded. Without it Submit simply downloads the file, which
# is the normal path — these sessions are run with a facilitator who asks for it.
#
# THE GOAL PARSER NEEDS A PUBLIC URL, or the model half of the language layer is
# dead on the published page: with GOAL_PARSER_URL unset the browser falls back
# to http://127.0.0.1:8000, which is the VISITOR'S own machine, where nothing is
# listening. Deploy worker/ and set the repository VARIABLE (not secret — it is
# a public URL, and secrets are unavailable to forks):
#   Settings -> Secrets and variables -> Actions -> Variables -> New variable
#   Name: GOAL_PARSER_URL   Value: https://<your-worker>.workers.dev
# Unset, the app still runs; typed goals just fall back to the offline
# dictionary, and the panel says so. See worker/README.md.

name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - "frontend/**"
      - "shared/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Let a run finish rather than cancelling it; queue anything that arrives mid-deploy.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - name: Build
        env:
          VITE_LOG_ENDPOINT: ${{ secrets.LOG_ENDPOINT }}
          VITE_GOAL_PARSER_URL: ${{ vars.GOAL_PARSER_URL }}
        run: npm run build
      - uses: actions/upload-pages-artifact@v3
        with:
          path: frontend/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
