{
  "name": "airflow-goal-parser",
  "private": true,
  "version": "0.1.0",
  "description": "Public plain-language goal parser for the airflow study app — holds the Anthropic key server-side so the deployed page can reach it.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.115.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260801.1",
    "typescript": "^5.6.3",
    "wrangler": "^4.118.0"
  }
}
